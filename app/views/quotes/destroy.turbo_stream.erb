<%= turbo_stream.remove dom_id(@quote) %>
<%= render_turbo_stream_flash_messages %>

<% unless current_company.quotes.exists? %>
  <%= turbo_stream.update dom_id(Quote.new) do %>
    <%= render "quotes/empty_state" %>
  <% end %>
<% end %>
